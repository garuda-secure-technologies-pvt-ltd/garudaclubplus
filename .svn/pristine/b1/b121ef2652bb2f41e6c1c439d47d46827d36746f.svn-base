package com.openbravo.pos.pda.struts.actions;

import com.openbravo.pos.pda.bo.Helper;
import com.openbravo.pos.ticket.ActiveFacility;

import java.util.ArrayList;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import com.openbravo.pos.ticket.UserInfo;
import java.util.HashMap;
/*
public class ActiveFacilityAction extends org.apache.struts.action.Action {

    private final static String SUCCESS = "success";
    private final static String FAILURE = "failure";

    @Override
    public ActionForward execute(ActionMapping mapping, ActionForm form,
            HttpServletRequest request, HttpServletResponse response)
            throws Exception {

        Helper mem = new Helper();
       
        UserInfo uinfo = null;
        uinfo = (UserInfo) request.getSession().getAttribute("user");
        

        ArrayList<ActiveFacility> activefacility = new ArrayList<ActiveFacility>();

        activefacility = (ArrayList<ActiveFacility>) mem.findActiveFacility(uinfo.getSearchkey());

        request.setAttribute("activefacility", activefacility);

        HashMap<String, String> map1 = new HashMap<String, String>();
        for (ActiveFacility af : activefacility) {
            map1.put(af.getMfuId(), af.getFacilityName());
        }
        request.getSession().setAttribute("map1", map1);

       

        return mapping.findForward(SUCCESS);



    }
}*/