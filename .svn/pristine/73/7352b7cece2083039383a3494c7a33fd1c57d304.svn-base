package com.openbravo.pos.pda.struts.actions;

import com.openbravo.pos.pda.bo.Helper;
import com.openbravo.pos.pda.struts.forms.DeactivateFacilityForm;
import com.openbravo.pos.ticket.CancelRequestInfo;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
/*

public class cancelRequestAction extends org.apache.struts.action.Action {

    private final static String SUCCESS = "success";
    private final static String FAILURE = "failure";

    @Override
    public ActionForward execute(ActionMapping mapping, ActionForm form,
            HttpServletRequest request, HttpServletResponse response)
            throws Exception {

        Helper hel = new Helper();
        CancelRequestInfo crinfo = null;
        String id=request.getParameter("id");       
        String s = request.getQueryString();
        //int i=hel.cancelRequestInfoHsql(s);
         int j=hel.cancelRequestInfo(id);
         if(j==1)
        return mapping.findForward(SUCCESS);
         else
             return mapping.findForward(FAILURE);
    }
}
*/